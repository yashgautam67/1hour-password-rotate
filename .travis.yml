language: minimal
os: linux
dist: bionic

env:
  global:
    - CI=true

before_install:
  - echo "Preparing build environment..."

install:
  - echo "No dependencies to install"

script:
  - chmod +x ./rotate-password.sh
  - ./rotate-password.sh
 

cache:
  directories:
    - $HOME/.cache

notifications:
  email: false
